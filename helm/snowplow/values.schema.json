{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "chart values",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "replicaCount": {
      "type": "integer",
      "description": "Number of snowplow pod replicas.",
      "title": "Replica Count"
    },
    "progressDeadlineSeconds": {
      "type": "integer",
      "description": "Deployment progressDeadlineSeconds; safety window for cold informer sync + prewarm.",
      "title": "Progress Deadline Seconds"
    },
    "strategy": {
      "type": "object",
      "description": "Deployment update strategy (RollingUpdate maxSurge/maxUnavailable). maxUnavailable:0 keeps the old warm pod serving while the new pod prewarms behind its prewarm-gated /readyz.",
      "title": "Deployment Strategy"
    },
    "image": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "registry": {
          "type": "string",
          "description": "Container image registry host. Overridden by global.imageRegistry when that is set (host-only; repository path preserved).",
          "title": "Registry"
        },
        "repository": {
          "type": "string",
          "description": "Container image repository.",
          "title": "Repository"
        },
        "pullPolicy": {
          "type": "string",
          "enum": [
            "Always",
            "IfNotPresent",
            "Never"
          ],
          "description": "Image pull policy.",
          "title": "Pull Policy"
        },
        "tag": {
          "type": "string",
          "description": "Pinned snowplow app image tag (decoupled from chart version).",
          "title": "Tag"
        }
      },
      "description": "Container image settings.",
      "title": "Image"
    },
    "imagePullSecrets": {
      "type": "array",
      "description": "References to secrets for pulling images from private registries.",
      "title": "Image Pull Secrets"
    },
    "nameOverride": {
      "type": "string",
      "description": "Override the chart name used in resource names.",
      "title": "Name Override"
    },
    "fullnameOverride": {
      "type": "string",
      "description": "Override the fully qualified resource name.",
      "title": "Fullname Override"
    },
    "serviceAccount": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "create": {
          "type": "boolean",
          "description": "Whether to create a service account.",
          "title": "Create"
        },
        "automount": {
          "type": "boolean",
          "description": "Automatically mount the service account API token.",
          "title": "Automount"
        },
        "annotations": {
          "type": "object",
          "additionalProperties": true,
          "description": "Annotations to add to the service account.",
          "title": "Annotations"
        },
        "name": {
          "type": "string",
          "description": "Name of the service account to use; generated if empty and create is true.",
          "title": "Name"
        }
      },
      "description": "Service account configuration.",
      "title": "Service Account"
    },
    "podAnnotations": {
      "type": "object",
      "additionalProperties": true,
      "description": "Annotations to add to the pod.",
      "title": "Pod Annotations"
    },
    "podLabels": {
      "type": "object",
      "additionalProperties": true,
      "description": "Labels to add to the pod.",
      "title": "Pod Labels"
    },
    "podSecurityContext": {
      "type": "object",
      "additionalProperties": true,
      "description": "Pod-level security context.",
      "title": "Pod Security Context"
    },
    "securityContext": {
      "type": "object",
      "additionalProperties": true,
      "description": "Container-level security context.",
      "title": "Security Context"
    },
    "service": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "ClusterIP",
            "NodePort",
            "LoadBalancer",
            "ExternalName"
          ],
          "description": "Service type.",
          "title": "Type"
        },
        "port": {
          "type": "integer",
          "description": "Service port exposing the main http listener.",
          "title": "Port"
        },
        "nodePort": {
          "type": "integer",
          "minimum": 30000,
          "maximum": 32767,
          "description": "Explicit NodePort (only honored when service.type=NodePort). Set by the installer to pin a deterministic, host-mappable port.",
          "title": "Node Port"
        }
      },
      "description": "Kubernetes Service configuration.",
      "title": "Service"
    },
    "probePort": {
      "type": "integer",
      "description": "DEPRECATED / no-op on the 1.0.x ship line: the binary has no dedicated probe listener; probes target the main http port. Retained for values-compatibility only -- setting it has no effect.",
      "title": "Probe Port"
    },
    "ingress": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable creation of an Ingress resource.",
          "title": "Enabled"
        },
        "className": {
          "type": "string",
          "description": "IngressClass name.",
          "title": "Class Name"
        },
        "annotations": {
          "type": "object",
          "additionalProperties": true,
          "description": "Annotations to add to the Ingress.",
          "title": "Annotations"
        },
        "hosts": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "host": {
                "type": "string",
                "description": "Hostname for the rule.",
                "title": "Host"
              },
              "paths": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "path": {
                      "type": "string",
                      "description": "URL path.",
                      "title": "Path"
                    },
                    "pathType": {
                      "type": "string",
                      "description": "Kubernetes Ingress pathType.",
                      "title": "Path Type"
                    }
                  }
                },
                "description": "Path rules for the host.",
                "title": "Paths"
              }
            }
          },
          "description": "Ingress host rules.",
          "title": "Hosts"
        },
        "tls": {
          "type": "array",
          "description": "TLS configuration for the Ingress.",
          "title": "TLS"
        }
      },
      "description": "Ingress configuration.",
      "title": "Ingress"
    },
    "resources": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "limits": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "cpu": {
              "x-kubernetes-int-or-string": true,
              "description": "CPU limit — a Kubernetes Quantity: integer cores or a string like \"500m\"/\"1\".",
              "title": "CPU"
            },
            "memory": {
              "type": "string",
              "description": "Memory limit.",
              "title": "Memory"
            }
          },
          "description": "Resource limits.",
          "title": "Limits"
        },
        "requests": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "cpu": {
              "x-kubernetes-int-or-string": true,
              "description": "CPU request — a Kubernetes Quantity: integer cores or a string like \"500m\"/\"1\".",
              "title": "CPU"
            },
            "memory": {
              "type": "string",
              "description": "Memory request.",
              "title": "Memory"
            }
          },
          "description": "Resource requests.",
          "title": "Requests"
        }
      },
      "description": "Compute resource requests and limits.",
      "title": "Resources"
    },
    "startupProbe": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "httpGet": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "path": {
              "type": "string",
              "description": "Path to probe.",
              "title": "Path"
            },
            "port": {
              "type": "string",
              "description": "Port (name or number) to probe.",
              "title": "Port"
            }
          },
          "description": "HTTP GET action for the probe.",
          "title": "HTTP Get"
        },
        "failureThreshold": {
          "type": "integer",
          "description": "Consecutive failures before giving up.",
          "title": "Failure Threshold"
        },
        "periodSeconds": {
          "type": "integer",
          "description": "How often to perform the probe.",
          "title": "Period Seconds"
        },
        "timeoutSeconds": {
          "type": "integer",
          "description": "Probe timeout.",
          "title": "Timeout Seconds"
        }
      },
      "description": "Startup probe configuration (targets the dedicated probe port).",
      "title": "Startup Probe"
    },
    "livenessProbe": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "httpGet": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "path": {
              "type": "string",
              "description": "Path to probe.",
              "title": "Path"
            },
            "port": {
              "type": "string",
              "description": "Port (name or number) to probe.",
              "title": "Port"
            }
          },
          "description": "HTTP GET action for the probe.",
          "title": "HTTP Get"
        },
        "periodSeconds": {
          "type": "integer",
          "description": "How often to perform the probe.",
          "title": "Period Seconds"
        },
        "timeoutSeconds": {
          "type": "integer",
          "description": "Probe timeout.",
          "title": "Timeout Seconds"
        },
        "failureThreshold": {
          "type": "integer",
          "description": "Consecutive failures before restart.",
          "title": "Failure Threshold"
        }
      },
      "description": "Liveness probe configuration (targets the dedicated probe port).",
      "title": "Liveness Probe"
    },
    "readinessProbe": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "httpGet": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "path": {
              "type": "string",
              "description": "Path to probe.",
              "title": "Path"
            },
            "port": {
              "type": "string",
              "description": "Port (name or number) to probe.",
              "title": "Port"
            }
          },
          "description": "HTTP GET action for the probe.",
          "title": "HTTP Get"
        },
        "periodSeconds": {
          "type": "integer",
          "description": "How often to perform the probe.",
          "title": "Period Seconds"
        },
        "timeoutSeconds": {
          "type": "integer",
          "description": "Probe timeout.",
          "title": "Timeout Seconds"
        },
        "failureThreshold": {
          "type": "integer",
          "description": "Consecutive failures before marking not ready.",
          "title": "Failure Threshold"
        }
      },
      "description": "Readiness probe configuration (targets the dedicated probe port).",
      "title": "Readiness Probe"
    },
    "autoscaling": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable autoscaling via HPA.",
          "title": "Enabled"
        },
        "minReplicas": {
          "type": "integer",
          "description": "Minimum number of replicas.",
          "title": "Min Replicas"
        },
        "maxReplicas": {
          "type": "integer",
          "description": "Maximum number of replicas.",
          "title": "Max Replicas"
        },
        "targetCPUUtilizationPercentage": {
          "type": "integer",
          "description": "Target average CPU utilization to scale on.",
          "title": "Target CPU Utilization Percentage"
        },
        "targetMemoryUtilizationPercentage": {
          "type": "integer"
        }
      },
      "description": "HorizontalPodAutoscaler configuration.",
      "title": "Autoscaling"
    },
    "args": {
      "type": "array",
      "description": "Command-line args for the snowplow container.",
      "title": "Args"
    },
    "volumes": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "description": "Volume name.",
            "title": "Name"
          },
          "configMap": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "name": {
                "type": "string"
              },
              "defaultMode": {
                "type": "integer"
              },
              "optional": {
                "type": "boolean"
              }
            }
          }
        }
      },
      "description": "Additional pod volumes beyond the chart-managed jq-modules ConfigMap.",
      "title": "Volumes"
    },
    "volumeMounts": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the volume to mount.",
            "title": "Name"
          },
          "mountPath": {
            "type": "string",
            "description": "Path at which to mount the volume.",
            "title": "Mount Path"
          },
          "readOnly": {
            "type": "boolean",
            "description": "Mount the volume read-only.",
            "title": "Read Only"
          }
        }
      },
      "description": "Additional container volume mounts.",
      "title": "Volume Mounts"
    },
    "extraEnvFrom": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "configMapRef": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "name": {
                "type": "string"
              },
              "optional": {
                "type": "boolean"
              }
            }
          }
        }
      },
      "description": "Additional envFrom entries appended to the chart-managed ConfigMap.",
      "title": "Extra Env From"
    },
    "initContainers": {
      "type": "array",
      "description": "Init containers for the pod.",
      "title": "Init Containers"
    },
    "nodeSelector": {
      "type": "object",
      "additionalProperties": true,
      "description": "Node selector for pod scheduling.",
      "title": "Node Selector"
    },
    "tolerations": {
      "type": "array",
      "description": "Pod tolerations for scheduling onto tainted nodes.",
      "title": "Tolerations"
    },
    "affinity": {
      "type": "object",
      "additionalProperties": true,
      "description": "Pod affinity/anti-affinity rules.",
      "title": "Affinity"
    },
    "env": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "LOG_LEVEL": {
          "type": "string",
          "description": "Log verbosity floor: debug, info, warn or error (case-insensitive; an unrecognised value falls back to info). Binary default is info; this chart sets warn. The single verbosity knob as of #163.",
          "title": "Log Level"
        },
        "DEBUG": {
          "type": "string",
          "description": "DEPRECATED (#163) — a thin alias for LOG_LEVEL=debug, kept only for back-compat. This chart no longer sets it; use LOG_LEVEL instead.",
          "title": "DEBUG (deprecated)"
        },
        "BLIZZARD": {
          "type": "string",
          "description": "Blizzard feature toggle.",
          "title": "BLIZZARD"
        },
        "JQ_MODULES_PATH": {
          "type": "string",
          "description": "Path where jq custom modules are mounted.",
          "title": "JQ Modules Path"
        },
        "GOMEMLIMIT": {
          "type": "string",
          "description": "Go soft memory limit; must be below the container memory limit.",
          "title": "GOMEMLIMIT"
        },
        "GOGC": {
          "type": "string",
          "description": "Go garbage collector target percentage.",
          "title": "GOGC"
        },
        "CACHE_ENABLED": {
          "type": "string",
          "description": "Enable the in-process MemCache.",
          "title": "Cache Enabled"
        },
        "CATALOG_UNSERVABLE_TTL_SECONDS": {
          "type": "string",
          "description": "Bounded-staleness backstop (seconds) for apistage catalog entries stored while their GVR informer is not servable. When > 0, such an entry self-evicts within this short TTL instead of the standard resolved-cache TTL. 0 disables (entries use the standard TTL).",
          "title": "Catalog Unservable TTL Seconds"
        },
        "UAF_RESOLVED_TTL_SECONDS": {
          "type": "string",
          "description": "Staleness bound (seconds) for restactions-class entries whose RESTAction declares a userAccessFilter stage (#118 (d)). When > 0, such an entry gets this short per-entry TTL instead of the standard resolved-cache TTL, capping how long an authorization change can be served stale; 0 disables. Scope limits: it bounds staleness over time, NOT identity, so it is not a mitigation for a cross-tenant key collision; and it stamps only the restactions class, not widgets or widgetContent. Inert only while the A-1 mitigation declines every userAccessFilter-bearing cache write; without that mitigation this value is live and shortens UAF-bearing restactions cells from the standard TTL to this one. Load-bearing again in 1.13.0.",
          "title": "UAF Resolved TTL Seconds"
        },
        "DEPS_RELIST_BRIDGE_TIMEOUT_SECONDS": {
          "type": "string",
          "description": "1.12.6 C2 relist delta bridge: seconds the bridge waits for a schema-relisted informer to sync before giving up (it then enqueues nothing; relist_bridge_timeout_total counts it and the 1.12.5 post-sync re-fire keeps covering the teardown window). Unset/unparseable falls back to 120.",
          "title": "Deps Relist Bridge Timeout Seconds"
        },
        "DEPS_RECONCILE_SAMPLE": {
          "type": "string",
          "description": "1.12.6 C3 sampled reconcile audit: resident L1 entries sampled per tick and probed against the informer indexer (ABSENT → handed to the dep-event worker). Bounded O(sample) under the store mutex. Unset/unparseable/non-positive falls back to 512.",
          "title": "Deps Reconcile Sample"
        },
        "DEPS_RECONCILE_PERIOD_SECONDS": {
          "type": "string",
          "description": "1.12.6 C3 sampled reconcile audit: seconds between ticks. \"0\" disables the periodic audit (GET /debug/reconcile stays available). Unset/unparseable falls back to 30.",
          "title": "Deps Reconcile Period Seconds"
        },
        "REFRESH_DROP_EVICT_MAX_PER_MINUTE": {
          "type": "string",
          "description": "1.12.6 breaker on refresher drop-point evictions. A background refresh that fails deterministically across the whole requeue budget (403/500/timeout/parse failure/not-servable content call) evicts the stale entry instead of leaving it resident until TTL, up to this many evictions per minute; over budget the key keeps the pre-1.12.6 drop-to-TTL and one WARN per window flags a suspected mass failure. 0 disables drop-point eviction for non-404 failures (1.12.5 behaviour). The self-object 404 route is not subject to it.",
          "title": "Refresh Drop-Evict Max Per Minute"
        },
        "RESOLVED_CACHE_MAX_ENTRY_AGE_SECONDS": {
          "type": "string",
          "description": "1.12.6 bounded L1 entry lifetime (seconds), measured from the FIRST Put under a key and not reset by refresh re-Puts (unlike RESOLVED_CACHE_TTL_SECONDS, which every Put resets). Past it the entry is evicted on Get and re-resolved from scratch — the backstop for a wrong refresh the event pipeline would otherwise keep re-serving. 0 disables.",
          "title": "Resolved Cache Max Entry Age Seconds"
        },
        "REFRESH_SUPPRESS_AFTER_DECLINES": {
          "type": "string",
          "description": "1.12.6: consecutive stage-error refresh declines of one L1 key before the key is marked refresh-by-traffic-only (no further background re-resolves or WARNs until a real Put or an eviction clears the marker). Structurally permanent declines (external endpoint, userAccessFilter cell, unsupported kind) suppress on the first occurrence. Minimum 1.",
          "title": "Refresh Suppress After Declines"
        },
        "OTEL_ENABLED": {
          "type": "string",
          "description": "Master switch for OpenTelemetry export to ClickStack (traces + metrics over OTLP/HTTP). \"false\" restores a byte-identical pre-1.12.4 process. Per-signal overrides: OTEL_TRACING_ENABLED, OTEL_METRICS_ENABLED, OTEL_LOGS_ENABLED.",
          "title": "OTEL Enabled"
        },
        "OTEL_EXPORTER_OTLP_ENDPOINT": {
          "type": "string",
          "description": "OTLP/HTTP collector endpoint. Default http://$(HOST_IP):4318 targets the node-local OTel collector daemonset agent (hostPort 4318), the only OTLP ingest on the platform; $(HOST_IP) is expanded by the Deployment. The collector *deployment* Service has no OTLP receiver — do not point here.",
          "title": "OTEL Exporter OTLP Endpoint"
        },
        "OTEL_TRACES_SAMPLER": {
          "type": "string",
          "description": "OTel SDK sampler family. Ship \"traceidratio\" (snowplow's own root decision). NOT \"parentbased_traceidratio\": the agent gateway samples 100% and propagates sampled=1, so a parent-based sampler would span 100% of customer /call.",
          "title": "OTEL Traces Sampler"
        },
        "OTEL_TRACES_SAMPLER_ARG": {
          "type": "string",
          "description": "Sampling ratio for OTEL_TRACES_SAMPLER (\"0.05\" = 5% of root traces). The SDK default with no sampler is 100% — always set together with OTEL_ENABLED.",
          "title": "OTEL Traces Sampler Arg"
        },
        "OTEL_LOGS_ENABLED": {
          "type": "string",
          "description": "OTLP log export (audit events). Ship \"false\": the collector agent's logs pipeline accepts filelog only, so an OTLP log export has no consumer. Logs stay stdout → filelog.",
          "title": "OTEL Logs Enabled"
        },
        "PRETTY_LOG": {
          "type": "string",
          "description": "Emit ANSI multi-line pretty logs when true; one-line JSON when false. Set false in production.",
          "title": "Pretty Log"
        },
        "PHASE1_TIMEOUT_SECONDS": {
          "type": "string",
          "description": "Absolute outer backstop (seconds) for the whole Phase 1 warmup. With snowplow Fix 1b this is a backstop, not the primary bound.",
          "title": "Phase 1 Timeout Seconds"
        },
        "PHASE1_SYNC_PASS_GRACE_SECONDS": {
          "type": "string",
          "description": "Per-pass grace (seconds) for one informer WaitForCacheSync pass; one stuck informer can stall at most this long, not the whole Phase 1 budget.",
          "title": "Phase 1 Sync Pass Grace Seconds"
        },
        "PHASE1_SYNC_QUIESCENCE_SECONDS": {
          "type": "string",
          "description": "Registered informer-set stability window (seconds) after which the Phase 1 barrier returns good-enough, deferring still-unsynced informers to lazy-register fallthrough and the refresher.",
          "title": "Phase 1 Sync Quiescence Seconds"
        }
      },
      "description": "Env vars rendered into the chart's snowplow ConfigMap and consumed via envFrom.",
      "title": "Env"
    },
    "jwt": {
      "type": "object",
      "additionalProperties": false,
      "description": "snowplow verifies RS256 JWTs issued by authn using authn's RSA public key, fetched from authn's JWKS endpoint (/.well-known/jwks.json) and cached. No key material is mounted: snowplow holds neither a private/signing key nor a copy of the public key, so rotating authn's keypair needs no snowplow redeploy. The key set is fetched lazily on first validation, not at startup, so snowplow does not depend on authn being up first.",
      "title": "JWT verification keys (JWKS)",
      "properties": {
        "jwksUrl": {
          "type": "string",
          "description": "Full JWKS URL. Empty derives it from URL_AUTHN as <URL_AUTHN>/.well-known/jwks.json; set only to point at a different authn.",
          "title": "JWKS URL"
        },
        "cacheTTL": {
          "type": "string",
          "description": "How long a fetched key set is served before it is refreshed (Go duration, e.g. \"5m\").",
          "title": "JWKS cache TTL"
        },
        "minRefreshInterval": {
          "type": "string",
          "description": "Minimum gap between two JWKS fetch attempts (Go duration, e.g. \"30s\"). Throttles the refetch an unknown \"kid\" triggers so it cannot become one fetch per request.",
          "title": "JWKS minimum refresh interval"
        },
        "requestTimeout": {
          "type": "string",
          "description": "Timeout for a single JWKS fetch (Go duration, e.g. \"5s\"). Also bounds how long a token validation can block.",
          "title": "JWKS request timeout"
        }
      }
    },
    "seedAuthn": {
      "type": "object",
      "additionalProperties": false,
      "description": "#57 prewarm seed→authn loopback-token artifacts (projected audience=authn SA token + authn ServiceAccount allowlist CR + seed-group ClusterRole/Binding). Default off; enabling requires the authn operator's serviceaccount.authn.krateo.io CRD.",
      "title": "Seed Authn",
      "properties": {
        "enabled": { "type": "boolean", "title": "Enabled" },
        "group": { "type": "string", "title": "Issued Group" },
        "authnNamespace": { "type": "string", "title": "Authn Namespace" },
        "tokenAudience": { "type": "string", "title": "Token Audience" },
        "tokenPath": { "type": "string", "title": "Token Path" }
      }
    },
    "global": {
      "type": "object",
      "additionalProperties": true,
      "description": "Helm global values (injected by the composition runtime).",
      "properties": {
        "imageRegistry": {
          "type": "string",
          "title": "Image registry",
          "description": "Registry-host override applied to every image (mirror / air-gapped installs). Replaces the per-image registry host; repository paths are preserved."
        }
      }
    }
  },
  "description": "Krateo PlatformOps Snowplow blueprint \u2014 full-coverage schema (chart 0.30.249)."
}
